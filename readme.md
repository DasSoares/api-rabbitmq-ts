# API com integração com RabbitMQ


## Crie arquivos env
A utilização de arquivos `.env`, `.env.dev` e `.env.prod` facilita o uso de variaveis para os ambientes de destino.
`Necessário criar as variavel de ambiente`